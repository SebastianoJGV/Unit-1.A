```.py
#Works in most languages
def rangePatterN (n : int):
  add = 0
  answer = []
  for i in range(100):
    answer.append(add)
    add+=1
    if add => n:
      add = 0
return (answer, "\n", sum(answer)

#Other option:
def rangePatterN (n : int):
  add = 0
  answer = []
  for i in range(100):
    answer.append(i%n)
    sum += i%n
    
return(answer, \n, sum
```
